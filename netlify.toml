[build]
  command = "npm run build"  # O el comando adecuado para tu backend
  publish = "public"  # La carpeta donde se publican los archivos listos para producción

[plugins]
  [[plugins]]
  package = "@netlify/plugin-functions-install-core"

[[redirects]]
  from = "http://*"
  to = "https://:splat"
  status = 301
  force = true
